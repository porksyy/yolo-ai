//using UnityEngine;

//public class BabyTester : MonoBehaviour
//{
//    public BabyAgent agent;

//    // Start is called once before the first execution of Update after the MonoBehaviour is created
//    void Start()
//    {
//        float[] observations = { 0.7f, 0.7f, 0.75f, 2f, 0.25f, 1f };
//        float[] output = agent.Predict(observations);
//        Debug.Log("Onnx model output: " + string.Join(", ", output));
//    }

//    // Update is called once per frame
//    void Update()
//    {
        
//    }
//}
